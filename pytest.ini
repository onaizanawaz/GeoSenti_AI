[pytest]
testpaths = tests
markers =
    gee: requires live Google Earth Engine credentials
    db: requires a live Postgres database
    llm: requires a live LLM endpoint (Ollama running, or an xAI key)
addopts = -m "not gee and not llm" -q